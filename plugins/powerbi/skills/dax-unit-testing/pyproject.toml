[project]
name = "dax-unit-testing"
version = "0.1.0"
description = "Measure certification registry scripts (setup, validate, generate, certify, coverage) for the dax-unit-testing skill."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.uv]
package = false
