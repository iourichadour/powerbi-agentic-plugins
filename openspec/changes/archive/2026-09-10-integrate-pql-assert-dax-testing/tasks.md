## 1. Skill Folder & Reference Assets

- [x] 1.1 Create `plugins/powerbi/skills/dax-unit-testing/` with `references/` and `assets/scripts/` subdirectories and create `plugins/powerbi/skills/dax-test-framework/` with `references/`, `assets/`, and `scripts/`; verify both trees exist
- [x] 1.2 Combine the selected PQL.Assert package/library TMDL files into a single `references/functions.tmdl` and verify it parses with valid indentation/tabs and contains `annotation DAXLIB_PackageId = PQL.Assert`
- [x] 1.3 Stage `references/model-independence.md` (from PQL.Assert's `model-independence/SKILL.md`) and `references/reserved-dax-words.md`, and verify both render as valid markdown with working relative links
- [x] 1.4 Create `references/certification-registry-schema.md` documenting the exact `MeasureCertification.csv` column schema (`MeasureName, TestName, TestCategory, FilterExpression, ExpectedValue, Tolerance, Owner, Status, ApprovalSource, ApprovedBy, ApprovedOn, Severity, RequirementId, LastReviewed`), the `TestCategory` definitions, the executable `Status` lifecycle, the `ApprovalSource` audit trail (`Structural`/`Developer`/`Business`, including the `ApprovalSource=Structural` ⇔ `TestCategory=Structural` cross-field rule and the `N/A` sentinel for `ApprovedBy`/`ApprovedOn` on structural approvals), the `FilterExpression` rule, and the full integrity-rule checklist (noting which rules require a live model and which run under `--schema-only`), and verify it satisfies the `powerbi/dax-unit-testing` spec's registry-schema and status-lifecycle requirements
- [x] 1.5 Add a legacy-registry migration reference documenting the verified 11-column legacy schema, explicit provenance mapping, structural-only automatic mapping, and rejection of ambiguous approved rows; verify fixtures cover Approved, Pending, and Retired legacy rows
- [x] 1.6 Add onboarding template assets to `dax-unit-testing/assets/templates/`: a starter `MeasureCertification.template.csv` (correct 14-column header plus one example row per `TestCategory`/`Status`/`ApprovalSource` combination, using only obviously synthetic placeholder values — never fabricated business figures — with `Approved` example rows carrying validation-legal `ExpectedValue`s such as `NOT_BLANK`/`>=0`/a synthetic numeric, `TBD` reserved for `Pending` rows, and `ApprovedBy`/`ApprovedOn` set to `N/A` on `ApprovalSource=Structural` rows) and a generalized `TESTING.template.md` (project-agnostic version of the reference model's testing guide: how PQL.Assert is deployed, naming convention, running tests locally and via the Python framework, and the measure-certification handoff); verify both templates contain no source-project names, measures, or live values and that the CSV template passes `validate_registry.py --schema-only` with zero findings

## 2. Registry Automation Scripts

- [x] 2.1 Implement `assets/scripts/validate_registry.py` enforcing every integrity rule (header match, `MeasureName`+`TestName` uniqueness, `MeasureName` exists in model, permitted `TestCategory`/`Status`/`ApprovalSource`/`Severity` values, the `ApprovalSource=Structural` ⇔ `TestCategory=Structural` pairing, non-empty `FilterExpression` unless `Structural`, `ExpectedValue` valid when `Approved`, `Tolerance >= 0`, ISO `ApprovedOn`/`LastReviewed` where required with `N/A` accepted only on `ApprovalSource=Structural` rows) and reporting failures as `REGISTRY_INVALID`; verify against valid, malformed, and quoted-comma-`FilterExpression` fixtures
- [x] 2.1a Add a `--schema-only` mode to `validate_registry.py` that applies every rule except `MeasureName`-exists-in-model and explicitly reports that rule as not evaluated, so registries and templates validate in CI without a model connection; verify it passes on `MeasureCertification.template.csv` and still fails the malformed fixtures
- [x] 2.2 Implement `assets/scripts/generate_measure_tests.py` converting `Status=Approved` rows into one `.dax` file per measure with a `DO NOT EDIT BY HAND` header, skipping `Pending` (reported as `CERTIFICATION_PENDING`) and `Retired` rows, and deterministically registering each generated file in `daxQueries.json` exactly once while preserving pre-existing hand-authored entries; verify two runs against identical fixture input produce byte-identical `.dax` **and** `daxQueries.json` output
- [x] 2.3 Add a content-hash guard to `generate_measure_tests.py` that fails as `GENERATED_FILE_MODIFIED` instead of overwriting a hand-edited file; verify by hand-editing a generated `.dax` file and re-running generation
- [x] 2.4 Implement `assets/scripts/certify_measures.py` with `scan` (read-only metadata compliance report: description, format string, display folder, home table, registry coverage) and `sync` (auto-generate + auto-approve `Structural` rows with `ApprovalSource=Structural` and `ApprovedBy`/`ApprovedOn` set to `N/A`, append `Status=Pending` `Certification` placeholders, support explicit developer certification of a reproducible baseline with `ApprovalSource=Developer`, flag but never auto-delete orphaned rows, record business certification only when explicitly supplied); verify both modes against a fixture model + registry
- [x] 2.5 Implement `assets/scripts/coverage_report.py` computing % measures with any registry row (vs. untested), executable `Structural` coverage, developer-certified and business-certified non-Structural coverage, and a `TestCategory` × `Status` × `ApprovalSource` breakdown, emitting `reports/coverage.json` and `reports/coverage-summary.md`; verify against a fixture model + registry with a mix of untested/pending/developer-certified/business-certified rows, confirming no writes occur to the registry or model
- [x] 2.6 Implement the legacy 11-column registry migration/import path, preserving values and refusing ambiguous approved provenance; verify structural rows map automatically and non-Structural approved rows require explicit mapping
- [x] 2.7 Implement `assets/scripts/setup_project.py` (one-time, idempotent, separate from `certify_measures.py` so that script keeps its read/report-and-registry-only contract): deploy the selected PQL.Assert functions into the target model's `functions.tmdl`, then copy `MeasureCertification.template.csv` to `Certification/MeasureCertification.csv` and `TESTING.template.md` to the semantic model project's `TESTING.md`, creating each only if it does not already exist and never overwriting a present file, and touching no other model object; verify a second run against an already-set-up fixture project leaves every destination byte-identical and reports what already existed, and that a partially set-up project gets only the missing file

## 3. DAX Test Framework Skill

- [x] 3.1 Extract the verified typed-row transport into `dax-test-framework/scripts/dax_test_helpers.py`, including dynamic Desktop port discovery, CLOUD environment configuration, UTF-8-sig query reads, `BLANK()` normalization, and strict `Passed` parsing; verify helper-level fixtures for port selection and result parsing
- [x] 3.2 Add `dax-test-framework/scripts/run_dax_tests.py` with `--profile`, `--model-dir`, `--environment`, `--file`, and `--reports-dir` options, mandatory smoke gate, distinct exit outcomes, the shared fixed error-type vocabulary on every failure (`CONNECTION_ERROR`, `DAX_ERROR`, `VALUE_MISMATCH`, `BLANK_RESULT`, `MEASURE_NOT_FOUND`) surfaced in console, JUnit XML, and Markdown output, and JUnit/Markdown output; verify batch and targeted fixture runs
- [x] 3.2a Add the `uv`-compatible dependency manifest (`pyproject.toml` plus lock) declaring the ADOMD.NET transport dependencies (`pythonnet`, `pyadomd`) and pytest for the framework scripts, aligned with the `add-python-powerbi-dependency-checking` change; verify `uv run` resolves the runner and pytest wrapper from a clean environment
- [x] 3.3 Add the required pytest wrapper and two clean notebook templates (`dax_tests_dev.ipynb`, `dax_tests_cloud.ipynb`) based on the reference behavior, with outputs/session metadata stripped and no hard-coded model name; verify JSON validity, shared helper usage, and explicit configuration cells
- [x] 3.4 Add a generic `assets/dax-test-dashboard.html` that fetches a configurable JUnit XML artifact with cache-busting, parses both `testsuite` and `testsuites`, renders summary/suite/assertion views, and falls back to a local XML file picker; verify it contains no embedded result rows and works against both sample XML shapes
- [x] 3.5 Add sanitized sample artifacts under `dax-test-framework/assets/samples/`: CLI JUnit XML, pytest JUnit XML, and Markdown failure summary; verify samples contain generic names only and are rendered by the HTML dashboard
- [x] 3.6 Author `plugins/powerbi/skills/dax-test-framework/SKILL.md` covering `uv run`, ADOMD.NET prerequisites, DEV/CLOUD selection, smoke-gate diagnostics, discovery and filters, CLI/pytest/notebook entry points, credential hygiene, report interpretation, local HTTP serving, and XML file-picker fallback; verify frontmatter and relative links
- [x] 3.7 Author `plugins/powerbi/skills/dax-unit-testing/SKILL.md` covering getting-started deployment via `setup_project.py` (library deployment plus `MeasureCertification.csv`/`TESTING.md` scaffolding from `assets/templates/`), DAX Query View test creation, environment governance (`ANY`/`DEV`/`PROD` filename environment tokens, matching the framework's filter set), assertion taxonomy, progressive registry workflow, coverage statistics, legacy migration, and fixed error vocabulary; verify frontmatter and relative links

## 4. `pql-tester` Agent

- [x] 4.1 Create `plugins/powerbi/agents/pql-tester.agent.md` configured for Claude Haiku 4.5 with tools `vscode`, `read`, `edit`, `agent`, `powerbi-modeling-mcp/*`, defining the explicit `setup`/`scan`/`sync`/`generate`/`run`/`report`/`diagnose` modes with no silent side effects between them; verify YAML frontmatter and mode definitions against the `powerbi/pql-tester-agent` spec's operating-modes requirement
- [x] 4.2 Define `setup` mode as the one-time, idempotent entry point that invokes `dax-unit-testing`'s `setup_project.py` to deploy the PQL.Assert library and scaffold `Certification/MeasureCertification.csv` and `TESTING.md` from the skill templates into the target semantic model project, creating each only if absent and never overwriting an existing file; verify against a fixture project with no prior setup and against one already partially set up
- [x] 4.3 Add progressive-certification guardrails (never invent `ExpectedValue`/`Owner`/`FilterExpression`/`RequirementId`; self-approve `Structural` rows with `ApprovalSource=Structural`; record a developer-approved reproducible baseline as `ApprovalSource=Developer`; record business values as `ApprovalSource=Business` only on explicit business approval) and verify them against the spec's progressive-certification requirement
- [x] 4.4 Add DEV/CLOUD dual execution guidance by routing execution to `dax-test-framework` (local Desktop vs. XMLA, identical artifacts, credentials from `PBI_WORKSPACE`/`PBI_MODEL`/`PBI_CLIENT_ID`/`PBI_TENANT_ID`/`PBI_CLIENT_SECRET` environment variables only, never logged) and verify against both framework and agent specs
- [x] 4.5 Add write-scope restriction (`Certification/`, `DAXQueries/`, `reports/`, `TESTING.md`, and the model's assertion-library definition file — the last only for `setup`'s PQL.Assert deployment — never any other model object), no-silent-failure-suppression guardrails (no tolerance widening, no retiring to silence, no deleting/disabling failing tests), and naming/structure conventions (`[Area].[Environment].Test(s)`, `UNION`-combined assertions, `daxQueries.json` maintenance); verify each against the corresponding `powerbi/pql-tester-agent` spec requirement

## 5. `powerbi-architect` Test-Task Planning

- [x] 5.1 Update `plugins/powerbi/agents/powerbi-architect.agent.md` to add `dax-unit-testing` to the Skills to use list, described as the source of the registry schema and assertion taxonomy for planning (not writing DAX)
- [x] 5.2 Add the progressive test-task-planning guideline (sync task → developer-certification → generate+run task, with optional additive business certification; calculated columns/layout-only tasks exempt) and verify against a spot-check spec containing a measure with no business value (baseline path remains executable) and one with a known business value (business certification is included but does not replace or block the baseline path)
- [x] 5.3 Add the one-time PQL.Assert library-setup prerequisite rule, specifying that the setup task invokes `pql-tester`'s `setup` mode (library deployment plus scaffolding `Certification/MeasureCertification.csv` and `TESTING.md` from the `dax-unit-testing` templates), and verify a spot-check spec whose first measure task targets an unequipped model includes exactly one setup task before the first `sync` task
- [x] 5.4 Update the Spec Template's Tasks section guidance/example to show the progressive task sequence and add a test-coverage/registry-status/approval-source callout line to the Measures/Calculations guidance under Components and Interfaces; verify the rendered template matches the `powerbi/architect-test-planning` spec's task-chain requirement

## 6. Ecosystem Wiring & Documentation

- [x] 6.1 Update `plugins/powerbi/README.md` to document the `pql-tester` agent, `dax-unit-testing` skill, and `dax-test-framework` skill, and verify the new entries render correctly
- [x] 6.2 Update `plugins/powerbi/agents/powerbi-developer.agent.md` to include both testing skills and delegate DQV test generation/execution to `pql-tester`; verify the routing references resolve
- [x] 6.3 Update `plugins/powerbi/skills/semantic-model-authoring/SKILL.md` to add a Workflow Selector row routing test requests to `dax-unit-testing`; verify the table entry is present and correctly formatted
- [x] 6.4 Update `AGENTS.md` and `CLAUDE.md` to register `dax-unit-testing`, `dax-test-framework`, and `pql-tester` in the skill/agent registries, and to note that the one-time `pql-tester setup` step scaffolds the target project's `TESTING.md` and starter `MeasureCertification.csv` for both the Copilot CLI and Claude Code agent surfaces; verify all entries are listed and the setup callout is present in both files
- [x] 6.5 Bump the `powerbi` plugin version and the root `metadata.version` in `.claude-plugin/marketplace.json` per repository convention for a feature-adding release, and verify the JSON parses and the versions are consistent with the rest of the file

## 7. Skill-Creator Evaluation and Rating

- [x] 7.1 Create `plugins/powerbi/skills/dax-test-framework/evals/evals.json` with three realistic execution-focused prompts — (a) run a DEV suite that hits a smoke-gate failure, (b) target a generated certification suite in CLOUD without exposing credentials, (c) review a failed run's JUnit XML in the HTML dashboard when automatic fetch is blocked — and `plugins/powerbi/skills/dax-unit-testing/evals/evals.json` with three realistic registry-focused prompts — (a) migrate/validate a legacy 11-column registry while refusing ambiguous approval provenance, (b) sync a model and certify a developer baseline without inventing a business value, (c) produce coverage statistics for a partially certified model; verify schema and expected-output descriptions
- [x] 7.2 Add objective assertions for each evaluation: smoke failure stops execution, no secret values appear in output, profile/file/environment flags are selected correctly, generated reports contain required fields, the HTML viewer loads XML dynamically and supports its fallback, legacy ambiguity is rejected, and no production model files are modified; verify assertions are discriminating against a no-skill baseline
- [x] 7.3 After the skill drafts exist, run with-skill and baseline evaluations in parallel, save timing metadata and grading results, aggregate benchmark pass-rate/token/time deltas, and run an analyst pass; verify `benchmark.json` and `benchmark.md` are produced
- [x] 7.4 Run `eval-viewer/generate_review.py` with the benchmark and present the Outputs/Benchmark review to the user; incorporate submitted feedback into one focused skill revision and rerun the affected evaluations before optimizing descriptions
- [x] 7.5 Optimize each skill description only after qualitative review, using a 20-query trigger set with should-trigger and near-miss should-not-trigger cases; verify the held-out score improves without increasing false positives

## 8. Final Verification

- [x] 8.1 Run the repository's existing validation for skill frontmatter, scripts, and JSON/notebook assets; verify all targeted checks pass without adding new tooling
- [x] 8.2 Run fixture tests for registry validation/generation/migration, framework discovery/filtering/reporting, and agent routing; verify expected error types and read-only boundaries
- [x] 8.3 Inspect `git status` and `git diff` to confirm only the intended plugin, documentation, evaluation, sanitized sample, and OpenSpec files are changed; verify no source-project model files, customer identifiers, or secrets were copied
- [x] 8.4 Run `openspec validate --strict` for this change and confirm no errors are reported
