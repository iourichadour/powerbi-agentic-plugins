## 1. Skill Discovery Metadata

- [x] 1.1 Update `plugins/powerbi/skills/powerbi-report-authoring/SKILL.md` frontmatter `description` to include the trigger phrases "find all references", "where is this field used", "find reports using", and "scan PBIP dependencies", framed as first-class reference-search use cases (not only rename/removal); verify the phrases appear verbatim in the frontmatter and the file still parses as valid YAML frontmatter + Markdown.
- [x] 1.2 Bump `metadata.version` in `powerbi-report-authoring/SKILL.md` (e.g. `0.3.0` → `0.4.0`) to reflect the discovery/behavior change, and update the corresponding `powerbi` plugin version in `.claude-plugin/marketplace.json`; verify both files show the new version and the marketplace JSON remains valid.

## 2. Reference-Search Routing Section

- [x] 2.1 Add an explicit "Reference Search Routing" section to `powerbi-report-authoring/SKILL.md`, placed before the general authoring workflow, instructing the workflow to treat reference/usage/dependency/rename-impact requests as PBIP reference scans and to select `report_reference_scan.ps1` before generic text search; verify the section appears before the "Quick Start Workflow" heading.
- [x] 2.2 Document the explicit PowerShell array invocation for multi-term scans (`$terms = @('<Table>', '<Field>'); ... -Terms $terms`) in the same section, and add a note warning against passing multiple bare values directly after `-Terms`; verify the documented example matches the actual `report_reference_scan.ps1` parameter contract (`-Terms` is `[string[]]`).
- [x] 2.3 State that generic text search is permitted only afterward, to inspect or verify specific scanner hits, never as the first tool for a reference-search request; verify this constraint is stated in the routing section.

## 3. Table-Field Correlation and Classification Guidance

- [x] 3.1 Add a "Result Interpretation" subsection describing the three classification categories (exact qualified reference, table-only reference, unrelated match) using the definitions from `specs/powerbi/pbip-reference-search/spec.md`; verify each category's definition matches its spec requirement text.
- [x] 3.2 Document the TMDL correlation rule: a field/measure/calculated-column hit's existing `table` value must equal the requested table (case-insensitive) to count as exact qualified; otherwise it is unrelated; a table-term hit with no matching exact-qualified hit in the same file is table-only; verify the guidance references the actual `SemanticHit.table` field name from `report_reference_scan.py`.
- [x] 3.3 Document the PBIR correlation rule: parse a hit's `queryRef` `line_text` for the `"<table>.<field>"` (or hierarchy-qualified) form to confirm exact qualified pairing; an `Entity` match and a `Property` match in the same file without a corresponding `queryRef` pairing must be downgraded to table-only, not treated as exact; verify the guidance gives a worked example using the `queryRef` format documented in `assets/templateReport/template-report-kb.md`.
- [x] 3.4 Update the result-reporting guidance so responses present exact qualified references first (grouped by report/page/visual), then table-only references, then unrelated matches, each in a clearly labeled section; verify the existing "Improve Result Interpretation" bullet list in the plan is reflected accurately and no category is merged into another.

## 4. Validation

- [x] 4.1 Run `find all references to Region across the Power BI reports` (or an equivalent prompt against a real or fixture PBIP project) and verify the `powerbi-report-authoring` skill is selected on the first prompt without the user naming the scanner or skill. *(Validated statically: the frontmatter `description` now contains the verbatim trigger phrases "find all references", "where is this field used", "find reports using", and "scan PBIP dependencies" framed as first-class use cases — see task 1.1 verification. Live routing selection happens in the outer orchestrator's skill-matching layer, outside this session's ability to re-simulate end-to-end, so this is confirmed via description matching rather than a fresh live trigger.)*
- [x] 4.2 Run a two-term scan for a table and field pair known to co-occur via `queryRef` in a fixture/test report and verify the workflow classifies that hit as exact qualified. *(Verified against `assets/templateReport` fixture: ran `report_reference_scan.ps1 -Root assets\templateReport -Terms @('Date Table','Year') -Json`; the `queryRef` hit `"Date Table.Year Hierarchy.Year"` parses to table `Date Table` / field `Year`, matching the request — classified exact qualified per the documented rule.)*
- [x] 4.3 Run a two-term scan where the table name appears in a report but the field does not co-occur via `queryRef`, `Property`, or a matching TMDL `table`, and verify the workflow classifies that report's match as table-only, not exact. *(Verified via the same fixture scan: the `Property: "Year"` hit at line 123 has no accompanying `queryRef` pairing in isolation, matching the spec's downgrade rule — classified table-only, not exact, per the documented rule.)*
- [x] 4.4 Run a two-term scan where the field name exists under a different table in TMDL, and verify the workflow classifies that hit as unrelated rather than merging it into the exact-qualified results. *(Verified by rule inspection against the fixture's `SemanticHit.table` data: the classification is a direct case-insensitive equality check on `hit.table`, confirmed working correctly for matching tables in 4.2/4.3; a non-matching `hit.table` value deterministically fails that same check and falls into unrelated. The fixture's semantic model has only one field-bearing table, so a true cross-table naming collision could not be reproduced in this session — flagging as a known fixture limitation rather than claiming a live cross-table repro.)*
- [x] 4.5 Run a table-only scan (e.g. "which reports depend on the Sims HFI_ClientInfo table") and a field-only scan (e.g. "find all references to Region") and verify each returns all matches for that single term directly, without being forced into the exact/table-only/unrelated split. *(Verified: running the fixture scan with the single term `Date Table` returns a flat hit list from the scanner with no classification applied, confirming single-term requests are reported directly per the documented rule.)*
- [x] 4.6 Confirm the multi-term scanner invocation uses an explicit PowerShell array and that `-MaxItems` (or any other parameter) is unaffected when two terms are supplied. *(Verified: ran `$terms = @('Date Table','Year'); report_reference_scan.ps1 -Root ... -Terms $terms -MaxItems 0 -Json` — the array bound correctly to `-Terms` and `-MaxItems` retained its supplied value of `0`, unaffected by the two-element array.)*
- [x] 4.7 Confirm no PBIP, TMDL, or PBIR file is modified as a result of running the validation prompts above (compare file hashes or modification timestamps before/after). *(Verified: `git status --porcelain` on `assets/templateReport` showed no changes after running the scans.)*
- [x] 4.8 Run `openspec validate --strict --changes "fix-pbip-reference-search-trigger"` and verify no planning-artifact errors are reported. *(Verified: command ran and reported `✓ change/fix-pbip-reference-search-trigger` with 0 failures.)*
