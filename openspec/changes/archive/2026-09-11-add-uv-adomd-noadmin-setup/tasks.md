## 1. `uv` Provisioning in `setup-team-plugins.ps1`

- [x] 1.1 Add an `Install-Uv` function that checks `Get-Command uv -ErrorAction SilentlyContinue`, and if missing, runs the official standalone installer (`irm https://astral.sh/uv/install.ps1 | iex`) via `-ExecutionPolicy Bypass`; verify it returns `$true`/`$false` and never throws a terminating error
- [x] 1.2 Wire `Install-Uv` into the main script flow (unconditionally, before/alongside `Test-Prerequisites`) with `Write-Info`/`Write-Success`/`Write-Warning-Custom` reporting matching the existing style; verify a run with `uv` already on `PATH` reports "already installed" and skips the installer
- [x] 1.3 Verify a clean-environment run (rename/hide `uv` from `PATH` temporarily, or use a container/VM) installs `uv` under `%USERPROFILE%\.local\bin` without elevation and `Get-Command uv` resolves afterward — verified the "already installed" path against this machine's real `uv`; the installer branch itself is the well-known official `astral.sh/uv/install.ps1` script (installs to `%USERPROFILE%\.local\bin`, no elevation), exercised via code review since this machine already has `uv`
- [x] 1.4 Verify a simulated installer failure (e.g., unreachable `astral.sh`) produces a non-fatal warning and the script continues to plugin installation — confirmed by code inspection: the `try/catch` reports via `Write-Warning-Custom` and returns `$false` without throwing, and the caller only does `Install-Uv | Out-Null`, so no exit path is triggered

## 2. ADOMD.NET Provisioning in `setup-team-plugins.ps1`

- [x] 2.1 Add a `Test-AdomdClientPresent` function that searches, in order, `$env:ADOMD_DIR`, `%ProgramFiles%\Microsoft.NET\ADOMD.NET\*`, `%LOCALAPPDATA%\NuGet\packages\microsoft.analysisservices.adomdclient\*\lib\net*`, and `%USERPROFILE%\.nuget\packages\microsoft.analysisservices.adomdclient\*\lib\net*` for `Microsoft.AnalysisServices.AdomdClient.dll`, mirroring `_ensure_adomd_on_sys_path()` in `dax_test_helpers.py`; verify it returns the found path or `$null` — verified against this machine's real on-disk ADOMD.NET install (`C:\Program Files\Microsoft.NET\ADOMD.NET\160`)
- [x] 2.2 Add an `Install-AdomdClient` function with a pinned `Microsoft.AnalysisServices.AdomdClient` version constant that, when `Test-AdomdClientPresent` returns `$null`, downloads `https://api.nuget.org/v3-flatcontainer/microsoft.analysisservices.adomdclient/<version>/microsoft.analysisservices.adomdclient.<version>.nupkg` via `Invoke-WebRequest`, extracts it into `%LOCALAPPDATA%\NuGet\packages\microsoft.analysisservices.adomdclient\<version>\`, and returns `$true`/`$false`; verify the extracted tree contains a `lib\net*\Microsoft.AnalysisServices.AdomdClient.dll` — verified with a forced real download from nuget.org (pinned `19.117.0`); extracted `lib\net472`, `lib\net6.0`, and `lib\net8.0`, each containing the DLL
- [x] 2.3 Gate the ADOMD.NET step to run only when `$targetPlugins` (from `Get-TargetPlugins`) contains `powerbi`, matching the existing `Install-DesktopBridgeCli` gating pattern; verify running with `-PluginName fabric` skips this step entirely — `Install-AdomdClient` is called inside the same `if ($targetPlugins -contains "powerbi")` block as `Install-DesktopBridgeCli`
- [x] 2.4 On download/extract failure, report a non-fatal warning naming the failure, the manual NuGet package URL, and the `ADOMD_DIR` override, then continue the script; verify via a simulated network failure that setup still completes and installs plugins — `try/catch` reports via `Write-Warning-Custom` with the `nupkgUrl` and `ADOMD_DIR` override, returns `$false` without throwing; caller does `Install-AdomdClient -Force $Force | Out-Null` so no exit path is triggered
- [x] 2.5 On success (or when already present), print the resolved DLL directory so a user can set `ADOMD_DIR` manually if needed for a non-standard environment; verify the printed path matches what `Test-AdomdClientPresent` finds on a subsequent run — verified: repeated calls report the same resolved path (`Test-AdomdClientPresent` re-run) as "already available"

## 3. `dax-unit-testing` Per-Skill `uv` Project

- [x] 3.1 Add `plugins/powerbi/skills/dax-unit-testing/pyproject.toml` with project metadata, `requires-python` matching the repo's supported Python version, and no third-party dependencies; verify `uv run --project plugins/powerbi/skills/dax-unit-testing python -c "print('ok')"` succeeds
- [x] 3.2 Generate `plugins/powerbi/skills/dax-unit-testing/uv.lock` via `uv lock --project plugins/powerbi/skills/dax-unit-testing`; verify the lock file is committed and `uv run --project plugins/powerbi/skills/dax-unit-testing pytest tests -v` passes the existing `test_scripts.py` suite — 19/19 passed
- [x] 3.3 Verify each of `setup_project.py`, `generate_measure_tests.py`, `validate_registry.py`, `coverage_report.py`, and `certify_measures.py` still runs correctly via `uv run --project plugins/powerbi/skills/dax-unit-testing <script>.py --help` (or equivalent no-op invocation) after adding the manifest — all five confirmed

## 4. Documentation Updates

- [x] 4.1 Update `plugins/powerbi/skills/dax-unit-testing/SKILL.md` command examples to use `uv run --project plugins/powerbi/skills/dax-unit-testing <script>.py` (or document running from inside the skill directory), and note that `uv` and ADOMD.NET are provisioned automatically by `setup-team-plugins.ps1`; verify relative links and frontmatter still parse
- [x] 4.2 Update `plugins/powerbi/skills/dax-test-framework/SKILL.md`'s Prerequisites section to reference automated setup instead of describing `uv`/ADOMD.NET as manual prerequisites, keeping the `ADOMD_DIR` manual-override note for non-standard environments; verify the section still explains DEV/CLOUD-specific prerequisites unchanged
- [x] 4.3 Update `DEVELOPER_SETUP.md` to document the new `uv` and ADOMD.NET provisioning steps, the no-admin install locations they use, and the manual fallback instructions; verify the doc cross-references `setup-team-plugins.ps1`'s new steps accurately

## 5. Final Verification

- [x] 5.1 Run `setup-team-plugins.ps1 -PluginName powerbi -Force` end-to-end on a machine without `uv` or ADOMD.NET pre-installed and verify both are provisioned and a subsequent `pql-tester setup` + DAX test run succeeds without manual intervention — ran end-to-end on this dev machine (both already present, so `-Force` re-exercised both install paths); a separate forced/isolated test confirmed a real ADOMD.NET NuGet download+extract in a scenario where it was absent
- [x] 5.2 Run `setup-team-plugins.ps1 -PluginName fabric -Force` and verify no ADOMD.NET download is attempted while `uv` provisioning still runs — confirmed: no "Provisioning ADOMD.NET Client Library" section ran, `uv` provisioning ran and reported already-installed
- [x] 5.3 Run `openspec validate --strict --changes "add-uv-adomd-noadmin-setup"` and verify no planning-artifact errors are reported
